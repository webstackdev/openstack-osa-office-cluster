# Route HAProxy logs (local0 facility) to a dedicated file.
# This prevents request logs from flooding /var/log/syslog
# and gives Promtail a clean file to scrape.

local0.*    /var/log/haproxy.log
& stop
