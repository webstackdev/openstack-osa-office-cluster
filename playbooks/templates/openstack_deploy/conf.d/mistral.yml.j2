---
# Mistral (Workflow) host mappings
# Deployed to /etc/openstack_deploy/conf.d/mistral.yml

# All Mistral services (API, engine, executor, notifier) run in LXC on the controller
mistral-infra_hosts:
  cloud-4core:
    ip: 192.168.50.168
